# Build artifacts
*.o
*.obj
*.exe
*.dll
*.a
*.lib
build/
bin/
out/

# Generated locally (paths match this machine's MSYS2 layout)
compile_commands.json

# IDE / OS
.vs/
.DS_Store
Thumbs.db
*.log
.env*

# Runtime / debug output (not part of the tree)
stderr.txt
stdout.txt
# DEBUG builds write collision/debug log (see sopwith.cpp: errorfile)
error

/debug-*.log

# Cursor local/agent state (can contain private artifacts)
.cursor/**
# Keep shared project guidance (optional)
!.cursor/rules/
!.cursor/rules/**
!.cursor/skills/
!.cursor/skills/**
# But still keep local Cursor config untracked
.cursor/cli.json

# Spec Kit local memory (often session-specific)
.specify/memory/
/sopwith3/*.rec
/sopwith3/*.tape
/sopwith3/*.sidecar
/sopwith3/*.rec.state.txt

/parity-harness/csharp/*
