<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>disable</ImplicitUsings>
    <Nullable>disable</Nullable>
    <!-- Sibling GameplayParityHarness/ is a separate project; do not compile its sources here. -->
    <DefaultItemExcludes>$(DefaultItemExcludes);GameplayParityHarness/**</DefaultItemExcludes>
  </PropertyGroup>
</Project>
